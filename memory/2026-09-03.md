# 2026-09-03

## AI response production sanity incident

- Current baseline: `VERIFIED FAIL` on production revision `2e103d04d543d029c5a5237d7a6259610af05b64`, policy `2026-09-01.15`.
- Owner incident: Main Ask accepted `who is hyrum smith`, waited too long, then returned a generic general-answer failure.
- Cross-surface live baseline: five remote questions, two 26-second aborts, 40 percent sampled failure rate, p95/max 26 seconds.
- Repair candidate: policy/cache `2026-09-03.16` / `20260903-16`.
- Candidate behavior: reusable Church-person classification, official-evidence identity upgrade, one browser request, one shared Worker deadline, bounded retry, explicit diagnostics, meaningful outage copy, and a five-question post-deploy live matrix.
- Owner content boundary: block profanity, explicit sexual content, and derogatory attacks on faiths, cultures, ethnicities, nationalities, and political affiliations before echo/history/network; use the approved focusChrist redirect. Route immediate abuse or danger to urgent safety guidance.
- Verification at this checkpoint: all local structural/runtime/source-policy suites pass. GitHub CI, deployment, live visitor journey, and Master Brain promotion are still pending and must not be described as verified.
- Durable artifacts: `docs/ai-response-production-sanity-prompt-2026-09-03.md`, `docs/ai-response-incident-root-cause-2026-09-03.md`, `docs/ask-surface-registry.md`, `tools/live_ai_response_matrix.js`, and `tools/question_safety_runtime_qa.js`.

## First production acceptance result

- Merge `179b8be3d0a4788ab88fc3fe48289a116d47f37c` deployed browser release `2026-09-03.16`; Pages and post-merge site QA passed. Cloudflare Worker deployment also succeeded.
- The new live matrix correctly failed the Worker workflow. Hyrum Smith received a 60-word unverified `general-ai-low-risk` answer because server identity correction depended on successful research. Scripture and Pioneer specimens received bounded `research-rate-limited` fallbacks. The gate prevented this from being mislabeled as resolved.
- Follow-up candidate uses Worker policy `2026-09-03.17`: classify known Church people on the server before research, preserve dynamic official-evidence correction for misspellings and unknown names, allow the one 429 retry to honor up to five seconds of provider delay within the 22-second total budget, and pace matrix specimens by three seconds to avoid a synthetic burst.

## Verifier token-limit finding

- Worker policy `2026-09-03.17` fixed Hyrum Smith: the live result was 3.6 seconds, 211 words, faith-study, verified, and official-only.
- Later matrix calls failed at verification with HTTP 429. An isolated Alma 32 request after the burst succeeded, so the verifier service was healthy but over its minute window.
- Official Groq free-plan limits are 8,000 TPM for `openai/gpt-oss-20b` and 70,000 TPM for Compound Mini. Sending up to six 1,800-character evidence excerpts per verifier call was incompatible with a five-question production gate and likely with modest multi-user traffic.
- Worker policy `2026-09-03.18` keeps separate research and verification but caps collected evidence at four 700-character excerpts and supplies only the first two eligible sources to verification. The complete evidence block is capped at 5,000 characters. The live matrix uses a five-second inter-question pause while preserving per-question 20/25-second latency gates.

## Cross-provider verifier candidate

- Policy `.18` production acceptance passed four of five specimens. Hyrum Smith, general science, Alma 32, and Pioneer irrigation were verified in 3.3 to 10.6 seconds; the final Relief Society question failed at the Groq verifier with HTTP 429 after 7.0 seconds.
- Official provider research rejected Duck.ai because it is documented as an end-user chat product without an official developer API. OpenRouter's no-credit free limit is 50 requests/day and its own FAQ says free models are usually unsuitable for production. Cerebras is time-limited trial credit. Gemini's unpaid API permits Google and human reviewers to use inputs and outputs for product improvement, so it is not the preferred public-question default.
- Cloudflare Workers AI is the strongest no-new-key lane: 10,000 free neurons/day, 300 text-generation requests/minute, native Worker binding, structured response support, and no training/service-improvement use of customer content without explicit consent.
- Candidate policy `2026-09-03.19` uses Groq Compound Mini for official-source research, Cloudflare `@cf/openai/gpt-oss-20b` as the primary verifier, and Groq 20B as one operational fallback. The identical verifier contract and deterministic final guards remain. Valid rejection never falls through to another verifier.
- Full multi-agent frameworks such as CrewAI, AutoGen, and MetaGPT remain candidates for background indexing, content QA, and scheduled brain workflows. They are orchestration software, not free inference, and should not add agent conversation latency to the real-time visitor path.
- Owner authorized lawful tools that create no user cost. This is permission to evaluate and install when justified, not to add unreviewed runtime dependencies. Direct DeepSeek, direct Kimi, and direct MiniMax APIs are paid; current Cloudflare-hosted flagship DeepSeek/Kimi variants do not improve the no-cost verifier choice. OpenRouter's MiniMax M3 free endpoint is reserved for offline/canary evaluation because OpenRouter documents free endpoints as limited and usually unsuitable for production.
- Exact `.19` live acceptance failed. Cloudflare handled general science, Alma 32, and Relief Society as primary, but produced unusable strict output for Hyrum Smith and Pioneer irrigation. Pioneer recovered through the one-shot Groq fallback; Hyrum fallback hit `json_validate_failed`, leaving an incomplete response. Cloudflare's official JSON Mode list excludes GPT-OSS 20B. Candidate `.20` switches only the Cloudflare verifier to the documented JSON-compatible Llama 3.3 70B fast model and keeps every other guard and fallback rule.
- Owner is willing to purchase MiniMax if a benchmark proves it works and is sufficient. Official MiniMax M3 API limits are 200 RPM and 10 million TPM, but the $22 Token Plan is described as personal projects/prototyping with rolling quota windows. Do not purchase or integrate until it passes the focusChrist evaluation set and its subscription terms/capacity are confirmed for the intended public service.
- Clean `.20` acceptance still failed. Llama 3.3 70B produced valid primary answers when it completed, but complex prompts crossed the 6.5-second primary ceiling. The one Groq fallback sometimes failed inside provider-side JSON enforcement before the Worker could validate it. Candidate `.21` allows 9 seconds for Cloudflare with the same 5-second fallback reserve, sends the fallback without provider-enforced `response_format` while retaining strict prompt JSON plus local parsing/shape guards, and waits 15 seconds after deployment before live acceptance to avoid stale policy propagation.
- NVIDIA Nemotron 3 Super through OpenRouter is a background-evaluation candidate, not a production dependency. It supports tool calling and JSON-schema output, but the free endpoint is rate limited, showed only 65.43 percent three-day availability when checked, and explicitly logs free-endpoint content for NVIDIA product improvement. It may be benchmarked for indexing, review, and nightly audits only after an account/key exists; do not silently send visitor conversations to it.

## Exact policy .21 production result

- Policy `.21` merged at production commit `1495e01c63158997a7467e3086f60984ff17891f`; Worker version `2c1831e5-9844-437c-99d4-fb0a6ddc8b55` deployed, and repository, Pages, and site QA workflows passed.
- The live Hyrum Smith specimen passed with a source-grounded, independently verified answer in 9,117 ms. The stable general-knowledge specimen returned a useful answer in 9,205 ms, but omitted the required resolved-profile and classification-mode receipt fields.
- The remaining Scripture, Pioneer, and Church History specimens failed at the research stage when Groq Compound Mini returned HTTP 429. The free plan's 250-request daily allowance is not sufficient as the sole arbitrary-question research owner after repeated testing or under future visitor traffic. Production remains `VERIFIED FAIL`.
- Durable next architecture: reviewed answers and a permitted local Church-source index first, domain-filtered retrieval fallback second, independent verification third. Tavily is the leading no-card retrieval candidate because its official plan provides 1,000 recurring free credits per month, domain filtering, and a zero-data-retention option. No Tavily, OpenRouter, or NVIDIA key was available in the runtime at this checkpoint.
- Next coherent policy must also add the missing general-answer receipt fields. Do not hard-code the live matrix questions or deploy a receipt-only revision while the underlying arbitrary-question retrieval failure remains.

## Exact policy .22 production result and .23 decision

- Policy `.22` merged at commit `c9288cb18352e74e95fea3678a95fe5a85d621a4`; Worker version `5051c511-0873-4cf3-a1ef-b84c240342f1` deployed in workflow `33714963009`. GitHub Pages run `33714963025` and site QA run `33714963085` passed.
- The official 900-URL Church index solved the Groq research bottleneck. All index-covered doctrine, scripture, Pioneer, and Church History specimens used `church-source-index`, official Church sources, and zero Groq research calls. General science answers also returned correct profile and classification receipts.
- The exact 15-core live matrix still failed six verifier specimens. Cloudflare Llama 3.3 70B intermittently timed out or rejected usable indexed evidence; the already depleted Groq fallback could not recover. Nine core specimens passed, with observed request times from 2,825 ms to 10,533 ms, but the run correctly stopped before the burst and capacity verdict. Status remains `VERIFIED FAIL`.
- Candidate `.23` changes only the primary verifier to the active `@cf/meta/llama-3.2-11b-vision-instruct`, which Cloudflare lists under that exact ID for JSON Mode and for pricing at 4,410 input and 61,493 output neurons per million tokens. The first 8B-fast idea was rejected before publication because its JSON-support ID and priced ID did not match. All source, safety, copyright, privacy, retry, and acceptance gates remain unchanged.

## Exact policy .23 production result and .24 decision

- Policy `.23` merged at commit `de684822daed4339cf6881690529c220c4ba2078` and deployed as Worker version `81128a24-9588-4079-a344-fd072183d7a5` in workflow `33715774873`. Repository QA and Pages passed.
- The 11B endpoint returned Cloudflare `service_unavailable` immediately on indexed prompts. Requests fell to Groq verification, where some succeeded but most faith specimens returned provider errors or valid rejections. The unchanged matrix correctly remained `VERIFIED FAIL`.
- The receipts also exposed a gateway semantic defect independent of the model: indexed evidence set `DRAFT` to a task instruction rather than a candidate answer. A strict verifier could reject that instruction as unsupported. Candidate `.24` sends an empty indexed draft and explicitly composes the final answer from evidence, returns to the proven 70B JSON-mode primary, raises only its bounded primary ceiling from 9 to 12 seconds, and reduces the normal output allowance from 700 to 500 tokens. All acceptance gates remain unchanged.

## Exact policy .24 production result and .25 decision

- Policy `.24` merged at commit `3a45a4c4718d5e011f8625e46b8745fa7354c9d5`. Pages run `33716404955` and site QA run `33716404952` passed. Worker run `33716404968` deployed the hardened Worker and completed all 15 paced core requests before the acceptance assertion.
- Thirteen core specimens returned complete answers. All indexed specimens used official Church sources and zero Groq research. The Cloudflare primary stayed available and returned approved JSON, but the final deterministic overlap guard rejected the baptism answer and one Relief Society answer because their wording reconstructed more than the permitted source-text allowance. This was a presentation-integrity failure, not missing evidence or provider availability. Production remains `VERIFIED FAIL`.
- Candidate `.25` keeps the overlap guard unchanged. The initial prompt now requests concise, independently worded paraphrase. If an approved answer fails depth or overlap, one combined Cloudflare-only repair rewrites it inside the existing deadline. Normal repair output is capped at 400 tokens, total Cloudflare calls remain at most two, and a Groq fallback can never be stacked with the repair. A new runtime test forces overcopied evidence and proves exactly one repair plus final verified output.

## Exact policy .25 production result and .26 decision

- Policy `.25` merged at commit `cf668ac3f21ec650f3179c27e7b0fb5494d1604c`; Worker version `e91b1d38-79a0-4e42-ac33-49bce4779bfb` deployed in run `33717103285`. Pages run `33717103228` and site QA run `33717103237` passed.
- Cloudflare remained available across all core specimens and indexed retrieval continued to make zero Groq research calls. The baptism answer was still rejected after 17,073 ms because the first approved answer accumulated more than 25 sparse two-word source matches and too little deadline remained for the optional repair. This is a false positive in the cumulative reconstruction heuristic, not missing evidence. A Pioneer answer also selected the legitimate official daily-life history page, but the test's URL expectation did not list that indexed path. Production remains `VERIFIED FAIL`.
- Candidate `.26` preserves the unconditional rejection of more than 25 consecutive copied words. Ordered two-word-or-longer fragments now fail only when their cumulative matched words exceed 25 and comprise at least 40 percent of the answer, distinguishing dense mosaic reconstruction from sparse common phrasing in a long independent answer. Synthetic two-, three-, four-, five-, and seven-word reconstruction tests still fail; a new low-density control passes. The Pioneer URL pattern now recognizes the indexed daily-life page, while the unchanged independent page fetch must still prove the expected irrigation and community concepts.

## Exact policy .26 production result and .27 decision

- Policy `.26` merged at commit `055b0513dcf33f1bf415146c3801817820242a37`; Worker version `4e57ff0d-99e8-4bb3-8c23-cd5f76cd302f` deployed in run `33717814014`. Pages run `33717814085` and site QA run `33717813682` passed.
- The baptism specimen passed with a 173-word official-only answer in 8,588 ms, proving the overlap-density correction removed the earlier false positive. Fourteen of 15 core specimens returned complete answers. One terse Pioneer irrigation paraphrase received a fast 2,226 ms false rejection even though indexed official evidence was present; the same production request succeeded on immediate replay with a 119-word answer and official evidence. Production remains `VERIFIED FAIL`.
- Candidate `.27` adds reusable Pioneer-page irrigation intent routing to the official Pioneer Settlements topic. It also permits exactly one Cloudflare-only reconsideration when an initial false verdict conflicts with at least two direct query terms in indexed official evidence. The prompt explicitly presumes neither approval nor rejection; the second verdict remains authoritative, uses no Groq fallback, shares the 22-second deadline, and keeps total Cloudflare calls at two or fewer. A runtime regression proves false-to-approved recovery and call accounting.

## Exact policy .27 production result and .28 decision

- Policy `.27` merged at commit `dfc28f17bc41d4e07401b1b6cabb978c745d122d`; Worker version `69d9af50-4b74-49f9-a225-7381b30cfba4` deployed in run `33718635865`. Pages run `33718635871` and site QA run `33718635856` passed.
- The Pioneer topic pin worked and 14 of 15 core specimens returned complete answers. The final grace specimen hit the 12-second Cloudflare primary ceiling and returned `verification-provider-error`. Its receipt showed one Cloudflare attempt, zero Groq calls, and the conservative 1,000-neuron fallback reservation. Production remains `VERIFIED FAIL`.
- Candidate `.28` retains the 22-second Worker deadline and 25-second browser ceiling. It increases the Cloudflare primary ceiling from 12 to 15 seconds, reduces the Groq fallback reserve from 5 to 3 seconds, and lowers the normal verifier output cap from 500 to 400 tokens. This reallocates time to the provider that completed 14 of 15 core requests, keeps one operational fallback opportunity, and does not weaken source, answer, safety, receipt, p95, or capacity gates.


## Exact policies .28 and .29 results and .30 fallback repair

- Exact `.28` merged at `a159970c8bc788e805aface76db48b8207afbda9` and deployed as Worker version `aae79062-4621-40b2-a584-0f42c093cccf` in run `33719411666`. All 15 core requests returned complete answers with required receipts and indexed questions used zero Groq research. The gate failed because grace selected broad Jesus Christ URLs.
- Exact `.29` merged at `9926fad955d2a317c853a9f4d7e01f2b14a91251` and deployed as Worker version `a02b3e67-b894-4a95-9abe-490a1e45cdb4` in run `33720321990`. Grace selected both official grace pages and 14 other core requests passed. The first cold Atonement request failed closed when the 70B primary produced no usable result and Groq did not recover. Production remains `VERIFIED FAIL`.
- Candidate `.30` replaces Groq only in the ordinary operational fallback position with Cloudflare's active, priced `@cf/meta/llama-3.1-8b-instruct-fp8-fast` model. It uses prompt-enforced JSON plus the strict parser, never follows a valid rejection, cannot trigger a third verifier call, and restores a 12/5-second split inside the 22-second deadline. Because a local timeout does not cancel the provider request, every Cloudflare call without usage contributes a conservative 1,000-neuron allowance. Measured and unresolved usage are added, including failed repair calls.


## Exact policy .30 production result and .31 source correction

- Policy `.30` merged at `1ff829fe371129b804c98498b4c5c5d4cb5de07b` and deployed as Worker version `469fe621-dcb4-4531-a59e-9e8cfb1bb68c` in run `33723103367`. Pages run `33723103277` and Site QA run `33723103338` passed.
- All 15 core questions returned complete answers with correct profiles and required receipts. The same-binding fast fallback recovered one Relief Society request in 13,231 ms, reporting two Cloudflare calls, eight measured neurons, and the 1,000-neuron conservative allowance. Indexed requests used official sources and zero Groq research.
- The gate failed during independent source-page validation. The Pioneer answers cited the official Pioneer Settlements page, whose rendered article text discusses settlements and limited water but does not directly contain irrigation or cooperation. Production remains `VERIFIED FAIL`.
- Candidate `.31` pins Pioneer-page irrigation questions to the official brief-history page whose rendered article directly states that Saints saw spiritual value in cooperative irrigation and economics. The source check, answer check, retrieval limits, provider path, safety rules, latency ceilings, and capacity gates remain unchanged.

### Exact policy .31 production result and .32 stable source correction

- Policy `.31` merged at `78c51b5a25d2f5010340d437633e9f65ae17f8ce` and deployed as Worker version `1573aec5-a34c-43b3-84c2-2380c1172cb9` in run `33723817077`. Pages run `33723817103` and Site QA run `33723817099` passed.
- All 15 core requests continued to return complete answers. The independent source-page gate still failed because the `/learn/history/a-brief-history` fetch was inconsistent: one round returned that directly relevant page, while two rounds fell back to Pioneer Settlements, whose article text does not state irrigation.
- Candidate `.32` pins the same bounded Pioneer irrigation intent to the already indexed `/study/manual/church-history-in-the-fulness-of-times/chapter-twenty-six` page. Its rendered official article states that leaders taught planting and irrigation, that the pioneers entered the Salt Lake Valley, and that they immediately established a crude irrigation system. The matrix admits this exact path while retaining its independent page fetch and both irrigation and community or settlement concept checks. Production remains `VERIFIED FAIL` until exact `.32` acceptance.

### Exact policy .32 production result and .33 bounded reconsideration repair

- Policy `.32` merged at `53696b4bbedee4ed796ee7052381f9b4f1d3b9de` and deployed in run `33724967642`. Pages run `33724967599` and Site QA run `33724967624` passed.
- Fourteen of 15 core requests returned complete answers. The stable chapter 26 source was fetched, cached, and returned on the Pioneer route. The second-round short Pioneer wording received a fast false verifier rejection in 2,399 ms despite the pinned official evidence, and no reconsideration occurred because the excerpt-to-query lexical overlap heuristic did not reach two terms for that wording. Production remains `VERIFIED FAIL`.
- Candidate `.33` reuses the same bounded Pioneer irrigation intent predicate for both ranking and reconsideration. A false verdict may receive exactly one Cloudflare-only reconsideration when the request is on the Pioneer page, contains irrigation or shared-water intent, and the retrieved evidence includes the exact pinned chapter 26 URL. Unrelated cooperation, general Ask, missing evidence, valid rejection outside this class, Groq fallback, a third call, and deadline expansion remain forbidden. A runtime regression forces the exact short paraphrase from false to verified and requires two Cloudflare calls, source integrity, and the chapter 26 source.

### Exact policy .33 production result and .34 evidence-cache correction

- Policy `.33` merged at `baf96977d56eb3d40894cd93f0d17fe06264542f` and deployed as Worker version `d13fda57-918c-4881-85df-24fdfe998c8c` in run `33726361113`. Pages run `33726361054` and Site QA run `33726361004` passed.
- All 15 core requests returned complete answers. The short second-round Pioneer request passed, but the release gate failed its selected-excerpt relevance receipt: the cached chapter 26 excerpt exposed only one normalized query overlap even though the independently identified official page contains the required irrigation and settlement evidence. Production remains `VERIFIED FAIL`.
- Candidate `.34` keeps the two-term receipt gate unchanged. It gives the exact bounded Pioneer irrigation pin a cache variant separate from the default URL cache, makes default packs query-independent, and prioritizes direct irrigation and Pioneer settlement paragraphs only inside that variant. The current query helps compact a fresh pinned page, and the versioned namespace prevents stale packs from surviving deployment. A default chapter warm therefore cannot suppress the later pinned fetch. Cache size, source count, fetch count, official-domain, HTML, deadline, provider, safety, answer, and capacity limits remain unchanged. Unit coverage warms the default variant first and proves the distinct pinned pack retains both concepts.
