# Approved Christ hero full-composition correction

## Owner-approved successor: September 7, 2026

Wyatt supplied `fully fully approved.png` and directed: "just use this attached one for now to update the site". This supersedes the earlier asset selection below, not the locked layout geometry.

- Current shared Home and all 13 Answer-detail hero asset: `assets/heroes/home-christ-signature-approved-20260907.png`.
- Exact uploaded PNG bytes, 2048 by 684, SHA-256 `4e9d4469bd9bd40d4e097eea887410a63f3c2f6dcc6ced3a3affd4813991b15b`.
- No regeneration, retouching, resampling, compression, or pixel changes during integration.
- Keep the original `home-christ-fully-approved.png` unchanged for recovery.
- Both the shared CSS hero and each full-size viewer link must use the new asset. Refresh the shared stylesheet cache key consistently.
- Preserve hero dimensions, background positioning, navigation, introduction text and spacing, pills, and unrelated page art.

The earlier execution prompt is retained below as historical context; its old filename is no longer the active hero selection.

## Locked owner execution prompt

Use the exact owner-approved `assets/heroes/home-christ-fully-approved.png` file on the Home page and on every individual Answer page that already uses the shared Christ hero. Do not regenerate, retouch, crop, stretch, compress, recolor, replace, or otherwise change the approved image bytes. The delivered file must remain the 2048 by 684 PNG with SHA-256 `823e46fe509f71f3d8dfc9dd277e50223e55e6c53e2af9af5d8c55b9c0a45384`.

Correct the presentation geometry so the complete image spans the entire viewport width on desktop and tablet while its full vertical composition remains visible, including Christ's hair, shoulders, robe, and lower mist. The unchanged image must be centered and proportionally scaled. It may not use `cover` on these heroes, and it may not leave side gaps. The hero element must use the exact 2048:684 aspect ratio.

Every site page must share the Home hero's exact proportional desktop/tablet height so the hero never bounces when navigating between pages. Every desktop menu must use the same 52-pixel height. The lower introduction must occupy the exact remaining viewport height so the next section never appears as a clipped strip at the bottom. Center its eyebrow, heading, copy, buttons, and pills in that remaining space on supported review sizes. No control or text may be clipped or hidden. Mathematically incompatible extreme-width/short-height windows and enlarged accessibility text must flow naturally rather than hide content.

On mobile, preserve the established 300-pixel hero box on every page. On Home and the thirteen Answer pages using the approved Christ image, preserve its approved close framing at `auto 120%` and `center 24%`.

Do not change page wording, links, pills, study content, dialogs, artwork behavior, or unrelated page heroes. Add permanent regression checks for exact asset identity, complete page coverage, shared hero-page markers, banned `cover` on the approved desktop/tablet hero selectors, consistent cache revisions, full-width aspect geometry, and opening visibility. Run all existing repository QA. Obtain independent source review before commit and independent rendered verification at mobile, standard desktop, and ultrawide proportions. Publish only after the gates pass, then verify the deployed files and record the release truth without claiming any check that was not performed.
